/**
 * AuthorBooks screens.
 *
 * Import screens from this barrel (`@/screens/AuthorBooks`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './AuthorBooksScreen';
export { default as AuthorBooksScreen } from './AuthorBooksScreen';
