/**
 * Settings screens.
 *
 * Import screens from this barrel (`@/screens/Settings`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default as NotificationSettingsScreen } from './NotificationSettingsScreen';
