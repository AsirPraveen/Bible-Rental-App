# Several deps (lucide-react-native, and previously @lottiefiles/dotlottie-react)
# still declare peer react <=18 while Expo SDK 54 requires react 19. They work
# fine at runtime; only npm's resolver objects. This tree was already installed
# this way -- making it explicit so `npm install` is reproducible for everyone.
legacy-peer-deps=true
