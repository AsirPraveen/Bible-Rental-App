/**
 * DiscussionForum screens.
 *
 * Import screens from this barrel (`@/screens/DiscussionForum`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default as ForumListScreen } from './ForumListScreen';
export { default as QuestionDetailsScreen } from './QuestionDetailsScreen';
