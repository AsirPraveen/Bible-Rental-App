/**
 * Bible screens.
 *
 * Import screens from this barrel (`@/screens/Bible`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './BibleScreen';
export { default as BibleScreen } from './BibleScreen';
export { default as GeneratedImagesScreen } from './GeneratedImagesScreen';
