/**
 * History screens.
 *
 * Import screens from this barrel (`@/screens/History`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './HistoryScreen';
export { default as HistoryScreen } from './HistoryScreen';
