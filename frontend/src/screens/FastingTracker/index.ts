/**
 * FastingTracker screens.
 *
 * Import screens from this barrel (`@/screens/FastingTracker`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './FastingTrackerScreen';
export { default as FastingTrackerScreen } from './FastingTrackerScreen';
