/**
 * WishList screens.
 *
 * Import screens from this barrel (`@/screens/WishList`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './WishListScreen';
export { default as WishListScreen } from './WishListScreen';
