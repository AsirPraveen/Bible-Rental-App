/**
 * OrgSettings screens.
 *
 * Import screens from this barrel (`@/screens/OrgSettings`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './OrgSettingsScreen';
export { default as OrgSettingsScreen } from './OrgSettingsScreen';
