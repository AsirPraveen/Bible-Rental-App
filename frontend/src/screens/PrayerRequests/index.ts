/**
 * PrayerRequests screens.
 *
 * Import screens from this barrel (`@/screens/PrayerRequests`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './PrayerRequestsScreen';
export { default as PrayerRequestsScreen } from './PrayerRequestsScreen';
