/**
 * HistoricalMaps screens.
 *
 * Import screens from this barrel (`@/screens/HistoricalMaps`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './HistoricalMapsScreen';
export { default as HistoricalMapsScreen } from './HistoricalMapsScreen';
export { default as MapViewerScreen } from './MapViewerScreen';
