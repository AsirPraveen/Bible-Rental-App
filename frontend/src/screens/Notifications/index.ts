/**
 * Notifications screens.
 *
 * Import screens from this barrel (`@/screens/Notifications`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './NotificationsScreen';
export { default as NotificationsScreen } from './NotificationsScreen';
