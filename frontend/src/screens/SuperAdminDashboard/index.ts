/**
 * SuperAdminDashboard screens.
 *
 * Import screens from this barrel (`@/screens/SuperAdminDashboard`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './SuperAdminDashboardScreen';
export { default as SuperAdminDashboardScreen } from './SuperAdminDashboardScreen';
export { default as SuperAdminOrgDetailScreen } from './SuperAdminOrgDetailScreen';
export { default as SuperAdminSongsTab } from './SuperAdminSongsTab';
