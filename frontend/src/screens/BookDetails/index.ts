/**
 * BookDetails screens.
 *
 * Import screens from this barrel (`@/screens/BookDetails`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './BookDetailsScreen';
export { default as BookDetailsScreen } from './BookDetailsScreen';
