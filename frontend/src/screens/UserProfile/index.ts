/**
 * UserProfile screens.
 *
 * Import screens from this barrel (`@/screens/UserProfile`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './UserProfileScreen';
export { default as UserProfileScreen } from './UserProfileScreen';
