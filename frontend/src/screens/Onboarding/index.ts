/**
 * Onboarding screens.
 *
 * Import screens from this barrel (`@/screens/Onboarding`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './OnboardingScreen';
export { default as OnboardingScreen } from './OnboardingScreen';
export { default as OnboardingView } from './OnboardingView';
