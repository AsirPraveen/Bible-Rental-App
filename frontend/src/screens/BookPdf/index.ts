/**
 * BookPdf screens.
 *
 * Import screens from this barrel (`@/screens/BookPdf`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './BookPdfScreen';
export { default as BookPdfScreen } from './BookPdfScreen';
