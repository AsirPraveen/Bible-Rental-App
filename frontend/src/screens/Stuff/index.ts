/**
 * Stuff screens.
 *
 * Import screens from this barrel (`@/screens/Stuff`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './StuffScreen';
export { default as StuffScreen } from './StuffScreen';
