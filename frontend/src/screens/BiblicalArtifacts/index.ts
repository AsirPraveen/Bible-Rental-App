/**
 * BiblicalArtifacts screens.
 *
 * Import screens from this barrel (`@/screens/BiblicalArtifacts`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './BiblicalArtifactsScreen';
export { default as ArtifactViewerScreen } from './ArtifactViewerScreen';
export { default as BiblicalArtifactsScreen } from './BiblicalArtifactsScreen';
