// ════════════════════════════════════════════════════════
//  INTEGRATION.ts  —  v2 Setup Guide
// ════════════════════════════════════════════════════════

// ── 1. Install required packages ─────────────────────────
// Run in your frontend/ directory:
//
//   npx expo install @react-native-async-storage/async-storage
//   npx expo install expo-av                          ← Voice Notes
//   npx expo install expo-notifications               ← Push Reminders
//   npx expo install expo-file-system                 ← Export to file
//   npx expo install expo-sharing                     ← Share file
//   npx expo install @react-native-community/datetimepicker  ← Time picker

// ── 2. Add notification permissions to app.json ──────────
/*
  "expo": {
    "plugins": [
      [
        "expo-notifications",
        {
          "icon": "./assets/notification-icon.png",
          "color": "#1565C0"
        }
      ]
    ]
  }
*/

// ── 3. Navigation screens ─────────────────────────────────
import MessageNotesScreen from '../screens/MessageNotes/MessageNotesScreen';
import NoteDetailScreen   from '../screens/MessageNotes/NoteDetailScreen';
import NoteFormScreen     from '../screens/MessageNotes/NoteFormScreen';

/*
  <Stack.Screen name="MessageNotes" component={MessageNotesScreen} options={{ headerShown: false }} />
  <Stack.Screen name="NoteDetail"   component={NoteDetailScreen}   options={{ headerShown: false }} />
  <Stack.Screen name="NoteForm"     component={NoteFormScreen}     options={{ headerShown: false }} />
*/

// ── 4. Folder structure ───────────────────────────────────
/*
src/
├── types/
│   └── messageNote.ts         ← Updated types (highlights, voiceNotes, reminders)
├── services/
│   └── messageNoteService.ts  ← Full CRUD + verse lookup + notifications + export
├── components/
│   └── MessageNoteCard.tsx    ← Compact card with highlight/voice/reminder pills
└── screens/
    └── MessageNotes/
        ├── MessageNotesScreen.tsx   ← List + Search + Compact filters
        ├── NoteDetailScreen.tsx     ← Rich view + Export + Voice playback
        └── NoteFormScreen.tsx       ← Form with rich text + verse lookup + highlights + voice + reminders
*/

// ── 5. Feature summary ────────────────────────────────────
/*
  ✅ Auto title (Category + Date) — no manual title/speaker needed
  ✅ Compact category chips (minimal padding)
  ✅ Rich text editor: **bold** _italic_ • bullets ==highlight==
  ✅ Verse lookup: type Book Chapter:Verse → fetch Tamil or English text
  ✅ Verse Highlights: Yellow=Important, Blue=Promise, Red=Warning + notes
  ✅ Voice Notes: record, play, delete — multiple per note
  ✅ Reminders: push notifications, optional daily repeat
  ✅ Export: Text file, Share via WhatsApp, Email
  ✅ Full CRUD on all features
*/