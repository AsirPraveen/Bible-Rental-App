/**
 * JoinOrg screens.
 *
 * Import screens from this barrel (`@/screens/JoinOrg`) rather than reaching
 * for the files directly, so a screen can be renamed or split without
 * touching the navigators.
 */
export { default } from './JoinOrgScreen';
export { default as JoinOrgScreen } from './JoinOrgScreen';
